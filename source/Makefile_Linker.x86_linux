# Project: ExVM Multiplatform

# Target
BIN      = bin/linker_x86

# Compiler settings
CXXFLAGS = -O3 -Wall -W -m32 -march=native -fomit-frame-pointer -fno-exceptions -fexpensive-optimizations -D_VM_HOST_OS=_VM_HOST_LINUX_X64 -D_GCC_USE_BUILTIN

# Makefile settings
ARCH     = x86_linux
MEXT     = $(ARCH)

include linkertest.make

