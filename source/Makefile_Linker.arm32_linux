# Project: ExVM Multiplatform

# Target
BIN      = bin/linker_arm64

# Compiler settings
CXXFLAGS = -O3 -Wall -W -march=native -fomit-frame-pointer -fno-exceptions -fexpensive-optimizations -D_VM_HOST_OS=_VM_HOST_LINUX_I386

# Makefile settings
ARCH     = arm32_linux
MEXT     = $(ARCH)

include linkertest.make

